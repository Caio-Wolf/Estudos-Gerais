// Objetos

// Tipos de Variáveis por Referência.

// Abaixo um exemplo de Objeto, que é uma junção de informações que podem ser acessadas por meio de suas propriedades.
const pessoa = {
    nome: "Caio",
    idade: 24,
    estaProgramando: true,
    sobrenome: "Costa",
    sexo: "Masculino"
};
// Key (chave) = nome, idade, estaProgramado, sobrenome
// Value (valor) = "Caio", 24, true, undefined
console.log(pessoa.nome) // Caio

// Objeto Vazio:
const objetoVazio = {};